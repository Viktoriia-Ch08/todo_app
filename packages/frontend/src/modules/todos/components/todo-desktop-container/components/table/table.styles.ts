import { css } from '@emotion/css';
import { fonts, fontSize } from '~shared/styles';

export const todoTableItem = css`
	font-family: ${fonts.workSans};
	font-size: ${fontSize.s};

	border-bottom: 1px solid;
`;

export const tableBtnWrap = css`
	width: 50px;
`;
