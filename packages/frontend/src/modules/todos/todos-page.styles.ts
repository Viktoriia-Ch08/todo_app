import { css } from '@emotion/css';
import { fontSize, fontWeight, fonts } from '~shared/styles';

export const notFoundTodosText = css`
	font-family: ${fonts.workSans};
	font-size: ${fontSize.l};
	font-weight: ${fontWeight.medium};
	text-align: center;
`;
